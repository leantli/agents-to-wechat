# Changesets

This directory contains release notes for user-visible changes.

When you change behavior that should appear in the next release, add a new
`.md` file here with a short summary and the package bump type.

Use:

```bash
npm run changeset
```

before opening a pull request for a public-facing change.
